{
  "version": 2,
  "root": "apps/web",
  "buildCommand": "mkdir -p src/utils/__generated__ && SKIP_WORKERD=true NODE_OPTIONS='--max-old-space-size=4096' DISABLE_ESLINT_PLUGIN=true CI=false yarn build:production",
  "outputDirectory": "build",
  "installCommand": "yarn install",
  "framework": "create-react-app"
}

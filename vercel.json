{
  "version": 2,
  "buildCommand": "mkdir -p apps/web/src/utils/__generated__ && cd apps/web && npm install -D env-cmd && SKIP_WORKERD=true NODE_OPTIONS='--max-old-space-size=4096' DISABLE_ESLINT_PLUGIN=true CI=false npm run build:production && mkdir -p ../build && cp -r build/* ../build/",
  "outputDirectory": "apps/build",
  "installCommand": "npm install --no-optional",
  "framework": "create-react-app",
  "env": {
    "SKIP_WORKERD": "true",
    "SKIP_PREFLIGHT_CHECK": "true",
    "DISABLE_ESLINT_PLUGIN": "true",
    "CI": "false",
    "GENERATE_SOURCEMAP": "false",
    "NODE_OPTIONS": "--max-old-space-size=4096"
  }
}

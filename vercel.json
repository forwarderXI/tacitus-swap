{
  "version": 2,
  "buildCommand": "bash vercel-patch.sh && cd apps/web && yarn build:vercel",
  "outputDirectory": "apps/web/build",
  "installCommand": "yarn install --ignore-engines --ignore-scripts --network-timeout 100000",
  "framework": "create-react-app",
  "env": {
    "SKIP_WORKERD": "true",
    "SKIP_PREFLIGHT_CHECK": "true",
    "DISABLE_ESLINT_PLUGIN": "true",
    "CI": "false",
    "GENERATE_SOURCEMAP": "false",
    "NODE_OPTIONS": "--max-old-space-size=4096"
  }
}

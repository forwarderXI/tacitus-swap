{
  "version": 2,
  "buildCommand": "cd apps/web && NODE_OPTIONS=\"--max-old-space-size=4096\" yarn prepare && CI=false yarn build:production",
  "outputDirectory": "apps/web/build",
  "installCommand": "yarn install --frozen-lockfile",
  "rewrites": [
    {"source": "/(.*)", "destination": "/"}
  ]
}
